<!--
Replace this comment with the pull request body. It becomes the commit body on main, so:

- Write each paragraph as one unwrapped line. Say why the change is needed and what a reader of
  the history could not learn from the diff.
- No headings, no checklists, no trailers (no Co-Authored-By, no Signed-off-by).
- The title becomes the commit header: `type(scope): subject`, lowercase, imperative, no trailing
  period, 66 characters or fewer.

Before asking for review: `npm run lint`, `npm run typecheck`, `npm run format` and `npm run test`
pass, and tests cover the change including its error paths.
-->
